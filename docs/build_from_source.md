# Build From Source

Rust and Cargo are required, along with a C compiler (gcc, clang, MSVC, etc). Install at [Rust Getting Started](https://rust-lang.org/learn/get-started/).

```sh
git clone https://github.com/virtci/virtci.git
cargo install --path .
```
